# 存档文件解析

本文档记录 DragonSword: Awakening（龙之剑觉醒，UE 5.3.2）存档文件的完整解析过程，包括：存档文件构成、`.db` 加密格式破解历程（密钥如何找到）、文件格式详解、数据表结构与 ID 映射规则。

---

## 1. 存档文件构成

游戏存档位于 `<游戏目录>\DS\Saved\SaveGames\<账户ID>\` 下，包含：

| 文件 | 大小 | 说明 |
|---|---|---|
| `<账号ID>_Slot1.db` | ~897 KB | **核心玩家数据**（角色/货币/装备/物品/任务/成就等），SQLCipher 加密的 SQLite 数据库 |
| `<账号ID>_Slot1.bak` | ~897 KB | 保存前一刻的备份，格式与 .db 相同（每次保存重新生成） |
| `SPack_Slot1.sav` | ~7 KB | UE5 标准 SaveGame（GVAS 格式），仅含 UI/收集类附加数据 |
| `ScreenShot_1.png` | ~40 KB | 存档缩略图 |
| `steam_autocloud.vdf` | 52 B | Steam 云存档元数据 |

> **结论**：真正可修改的玩家数据全部在 `.db` 中；`.sav` 只有少量附加数据（下文 2 节）。

---

## 2. SPack_Slot1.sav（UE5 GVAS 格式）

标准 UE5 `USaveGame` 序列化（`FSaveGameHeader`），无自定义加密，可直接按 UE 存档规范解析：

```
GVAS                        ← Magic (4 字节)
03 00 00 00                 ← SaveGameFileVersion = 3
0A 02 00 00                 ← PackageFileUEVersion(UE4) = 522
F1 03 00 00                 ← PackageFileUEVersion(UE5) = 1009
<自定义版本 GUID 记录列表>    ← 若干条 16 字节 GUID + int32 版本
24 00 00 00 "/Script/DSClient.ClientSaveManager"  ← 存档对象类名
<属性列表>                    ← TaggedProperty 序列化
```

属性列表（全部为 `TMap<FString, ...>`，键为字符串）：

| 属性 | 值类型 | 内容 |
|---|---|---|
| `IntData` | map\<str, int32\> | 最近游玩副本、大师技能页签等状态键值（如 `107114809_Slot1_LAST_PLAY_DUNGEON_GROUP_5`） |
| `LongData` | map\<str, uint64\> | 随机大数键 → uint64 值 |
| `FloatData` | map\<str, float\> | 如 `Camera_Distance` |
| `StringData` | map\<str, str\> | 如 `CONTENTS_LOCK_EFFECT_UID_107114809_Slot1` = `"30013,70001,..."` |
| `SaveAccountTitleData` | struct | 书签/新称号 ID 数组 |
| `SaveItemCollectionData` | struct | 已收集物品 ID 数组 + 类型枚举 |
| `SaveQuestNavigationData` | struct | 主线/角色任务 ID |

**特点**：不含角色等级、金币、装备等核心数据——那些全在 `.db`。

---

## 3. `.db` 破解历程：密钥是如何找到的

### 3.1 格式识别

对游戏主程序 `DSClient-Win64-Shipping.exe`（162 MB）做字符串扫描，发现大量 SQLCipher 专属字符串（`sqlcipher_openssl_cipher: EVP_CipherInit_ex ...`、`PRAGMA cipher_compatibility = 4;` 等）→ 判定 `.db` 为 **SQLCipher（加密 SQLite）**。

同时发现 `FDsDBHandler` 类及其 50+ 个 Load 方法（`LoadCharacterInfoList`、`LoadCurrencyInfoList`、`LoadEquipmentItemInfoList`、`LoadStackableItemInfo`...），完整暴露了存档表结构。

### 3.2 静态定位关键函数

SQLCipher 静态链接进 exe，无导出符号。通过"日志字符串 xref"静态定位：

1. 在 exe 中查找 `sqlite3_key_v2: db=%p zDb=%s` 字符串的文件偏移
2. 扫描 `.text` 段，找 `LEA reg, [rip+disp32]` 指向该字符串的指令（即函数内引用点）
3. 从引用点向上找函数入口（`CC` 填充 + 典型序言）

得到关键函数 RVA（运行时 = 模块基址 + RVA）：

| 函数 | RVA | 用途 |
|---|---|---|
| `sqlite3_key` | 0x3CDF070 | 设置密钥（入口） |
| `sqlite3_key_v2` | 0x3D55F10 | 设置密钥（带库名） |
| `sqlcipherCodecAttach` | 0x3CDE895 | 真正接收密钥的底层函数 |
| `sqlcipher_cipher_ctx_key_derive` | 0x3CDFBA0 | 密钥派生（可读 kdf_iter 等参数） |
| `sqlcipher_openssl_kdf` | 0x3CE6390 | PBKDF2 层（派生主密钥/HMAC 密钥） |
| `sqlcipher_openssl_hmac` | 0x3CE6170 | HMAC 计算层（读真实 hmac_key/输入） |

### 3.3 动态提取密钥（Frida）

游戏无反作弊，使用 Frida 附加进程并挂钩上述函数：

- **第 1 步**：hook `sqlite3_key_v2`，`onEnter` 读取参数 `(db, zDbName, pKey, nKey)` → 捕获到 **口令 `13314374259236352028`**（20 字节 ASCII 数字串）。
- **第 2 步**：同时 hook `CreateFileW` 记录打开的文件路径，时间线确认该口令正是用于打开 `107114809_Slot1.db`（同一毫秒内调用）。
- **第 3 步**：用该口令离线解密页 1 → 得到有效 SQLite 头（`10 00 01 01 50 40 20 20`：页大小 4096、保留字节 80）→ **密钥确认**。
- **第 4 步**：但 HMAC 校验始终不匹配 → hook `sqlcipher_openssl_kdf`（PBKDF2 层）观察派生参数，发现 HMAC 密钥派生使用了**独立的 hmac_kdf_salt**（`39b00267...`，与文件头 salt 不同）。
- **第 5 步**：对比 SQLCipher 4.6.1 官方源码确认：

  ```c
  /* SQLCipher 4.x: hmac_kdf_salt = kdf_salt 逐字节 XOR 0x3A */
  memcpy(ctx->hmac_kdf_salt, ctx->kdf_salt, ctx->kdf_salt_sz);
  for (i = 0; i < ctx->kdf_salt_sz; i++)
      ctx->hmac_kdf_salt[i] ^= hmac_salt_mask;   // 0x3A
  ```

  验证：文件头 salt `038a385de1799411602af5b8c2513aec` XOR 0x3A = `39b00267db43ae2b5a10cf82f86b00d6` = 捕获的真实 hmac_kdf_salt ✅

### 3.4 最终加密参数（全部确认）

| 参数 | 值 |
|---|---|
| 口令（passphrase） | `13314374259236352028`（20 字节） |
| KDF 算法 | PBKDF2-HMAC-SHA512 |
| KDF 迭代（kdf_iter） | 256000 |
| HMAC 密钥派生迭代（fast_kdf_iter） | 2 |
| HMAC 密钥盐（hmac_kdf_salt） | `kdf_salt` 逐字节 XOR `0x3A` |
| 加密算法 | AES-256-CBC |
| 页大小 | 4096 字节 |
| 保留区（reserve） | 80 字节 = IV(16) + HMAC-SHA512(64) |
| HMAC 输入 | 密文 + IV + 页号（4 字节小端） |

### 3.5 写回（重加密）链路

```
解密:  salt = file[0:16]
       key = PBKDF2-SHA512(pass, salt, 256000, 32)
       hmac_key = PBKDF2-SHA512(key, salt^0x3A, 2, 32)
       每页: AES-256-CBC 解密(file[页起始+16(仅页1) : 页尾-80], IV=file[页尾-80:页尾-64])
       页1 重建 SQLite 头 "SQLite format 3\0"

加密:  每页新随机 IV(16)
       ct = AES-256-CBC(明文)
       hmac = HMAC-SHA512(hmac_key, ct + iv + 页号LE)
       页1: [salt][ct][iv][hmac], 其余页: [ct][iv][hmac]
```

实现见 `ds_save.py`（`decrypt_file` / `encrypt_pages` / `derive_keys`）。

---

## 4. `.db` 文件格式详解

### 4.1 物理结构（每页 4096 字节）

```
页 1:  [salt 16B][AES-256-CBC 密文 4000B][IV 16B][HMAC-SHA512 64B]
页 N:  [AES-256-CBC 密文 4016B][IV 16B][HMAC-SHA512 64B]
```

- **salt**：16 字节，文件头明文，用于派生密钥
- **IV**：每页随机 16 字节，存于页尾保留区前 16 字节
- **HMAC**：每页 64 字节（SHA512），覆盖 `密文 + IV + 页号(4B LE)`
- **保留区**：80 字节（16 IV + 64 HMAC），SQLite 头 offset 20 记录保留字节数 = 0x50

### 4.2 SQLite 结构

解密后为完全正常的 SQLite 数据库：**105 张表**（含索引等 197 个 sqlite_master 条目），页大小 4096。

### 4.3 关键数据表

| 表 | 内容 | 关键字段 |
|---|---|---|
| `tb_user` | 玩家信息 | USER_DBID(1000), POS_*, CUR_STAMINA |
| `tb_character` | 角色 | CHARACTER_CID, LEVEL, EXP, ASCEND(飞升), TRANSCEND(超越), HP |
| `tb_currency` | 货币 | ITEM_CID(1000001=金币, 1000002=以太晶体), AMOUNT |
| `tb_equipment` | 装备(164件) | ITEM_DBID, ITEM_CID, ENCHANT_LEVEL, MAIN_STAT_CID, SUB_STAT_CID1~5 |
| `tb_stackable_item` | 堆叠物品(158种) | ITEM_CID, STACK_CNT |
| `tb_skill_growth` | 技能等级 | CHARACTER_CID, TYPE_VALUE(技能槽), SLOT_LEVEL |
| `tb_gem` / `tb_karma` / `tb_cook_item` / `tb_vehicle` / `tb_costume` | 宝石/Karma/料理/坐骑/外观 | - |
| `tb_quest_*` / `tb_achievement_*` / `tb_title` / `tb_team` 等 | 进度/成就/称号/队伍 | - |

---

## 5. ID → 名称映射（11 语言）

### 5.1 数据链路

```
物品: GameItemData.table (ITEM_CID) → Name(StringData ID) → StringData.Zh_CN/En/...
角色: PCCharacterData.table (CID)  → Name(StringData ID) → 同上
词条: Equipment(Main|Sub)StatData  → StatList(属性) → StatListData.Name → 同上
技能: PCSkillGrowthData (TypeValue) → Name(StringData ID) → 同上
```

> StringData 主表每条含 11 种语言字段（SourceString=韩文, En, Ja, Zh_CN, Zh_TW, De, Fr, Es_ES, Pt_BR, Ru, Th），因此映射文件天然支持多语言。

### 5.2 数据文件（工具根目录）

| 文件 | 内容 |
|---|---|
| `id_names.json` | 物品/角色/怪物/NPC/坐骑/描述 → 11 语言名 |
| `stat_names.json` | 属性类型/主词条/副词条 → 11 语言名 |
| `skill_names.json` | 角色技能槽 → 名称 + 等级上限 |
| `level_exp.json` | 等级 → 合法经验值（改等级时自动匹配） |
| `equip_items.json` | 装备基底列表（系列/部位/品质） |

### 5.3 游戏规则（来自数据）

- **技能等级上限**：按技能槽位——槽 5-8 上限 10，槽 1/2/4 上限 7，闪避固定 1，大师技为点亮制（0/1）
- **等级经验**：`LEVEL=L → EXP = PCCharacterLevelData[L-1].CharacterExp`（EPIC 与 LEGENDARY 相同）
- **装备词条系列**：装备 ITEM_CID 前 3 位 - 134 = 词条前缀（136→2、137→3、138→4、139→5）
- **装备部位**：Category 1300=头盔、1301=上衣、1302=下衣、1303=手套、1304=鞋

---

## 6. 工具链

- **FModel**（CUE4Parse）：解包游戏 pak（本游戏 pak 无加密，可直接打开）
- 数据表导出路径：`Content/Design/GameData/*.table`（FModel "Export Data as JSON"）
- 生成映射文件的脚本位于开发机临时目录（`build_i18n_names.py` 等），可参考 5.1 链路重新生成

> 免责声明：本文档仅用于学习与研究存档格式。修改存档可能影响游戏体验，请自行备份。
